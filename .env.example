# Browser requests use the Vite proxy by default. Set an absolute URL for a separated deployment.
VITE_API_BASE_URL=/api/v1

# Development-only target used by vite.config.js.
SNOW_GRASS_PROXY_TARGET=http://127.0.0.1:8000

