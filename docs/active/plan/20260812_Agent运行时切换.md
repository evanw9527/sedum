# Agent 运行时切换 — 实施 Plan

- Spec：`docs/active/spec/20260812_Agent运行时切换.md`
- 前端仓库：`/Users/tb/Documents/sedum`
- 后端仓库：`/Users/tb/Documents/snow-grass`
- 分支：`third-multi-agent`
- 任务总数：25

## 模块 0：统一 Tool 基础设施（P0）

- [x] 0.1 修改 `src/snow_grass/tools/registry.py#ToolDefinition` — 增加参数 Schema、风险等级和结果类型
  - 范围：`ToolDefinition`、`ToolRegistry.register/list_allowed/execute`
  - 改动：建立 Runtime 无关的工具定义和参数校验入口。
  - Spec 引用：8.6 阶段 A；功能点 6。
  - 验收：AC4、AC5。
- [x] 0.2 新增 `src/snow_grass/agent/tool_adapter.py#ToolInvocationContext/BoundTool` — 定义调用上下文和绑定工具协议
  - 范围：新增数据模型与协议，不包含具体工具执行。
  - 改动：携带 Skill、会话、Run、Runtime 和刷新意图。
  - Spec 引用：8.6 目标结构；功能点 4、6。
  - 验收：AC4、AC7。
- [x] 0.3 新增 `src/snow_grass/agent/tool_adapter.py#SkillScriptToolAdapter` — 将当前 Skill 包脚本绑定为标准 Tool
  - 范围：动态生成 `run_skill_script` Schema 和当前 Skill 脚本白名单。
  - 改动：保留旧 Skill 脚本兼容，不允许任意 Python。
  - Spec 引用：8.6 阶段 A；功能点 6。
  - 验收：AC4、AC5。
- [x] 0.4 新增 `src/snow_grass/agent/tool_adapter.py#ToolDispatcher` — 统一权限、缓存、执行和审计结果
  - 范围：`resolve_tools`、`execute` 与标准工具事件数据。
  - 改动：普通 Tool 与脚本 Tool 共用分发入口。
  - Spec 引用：8.6 阶段 A；功能点 4、6。
  - 验收：AC4、AC5、AC7。
- [x] 0.5 修改 `src/snow_grass/skills/executor.py#SkillScriptExecutor` — 收缩为底层受控 Python 执行器
  - 范围：移除模型 Tool 构造职责，保留脚本发现、校验和进程执行。
  - 改动：明确该组件不是任意 Python Tool 或 OS 沙箱。
  - Spec 引用：8.6 当前问题与阶段 A。
  - 验收：AC4、AC5。
- [x] 0.6 修改 `src/snow_grass/agent/runner.py#AgentRunner.stream` — 所有 Tool Call 进入统一 Dispatcher
  - 范围：工具列表、循环执行、事件生成；删除具体工具名特判。
  - 改动：使普通 Tool 真正传入模型并可执行。
  - Spec 引用：8.6 阶段 A；功能点 4、6。
  - 验收：AC4、AC5、AC6。
- [x] 0.7 修改 `src/snow_grass/skills/validation.py#SkillValidator` — 增加脚本 Tool 声明与旧包兼容校验
  - 范围：`scripts/*.py`、`manifest.tools.allowed` 校验。
  - 改动：新包显式声明；旧包保留行为并给出迁移提示。
  - Spec 引用：8.6 阶段 A。
  - 验收：AC2、AC4。
- [x] 0.8 修改 `src/snow_grass/main.py#create_app` — 初始化 ToolDispatcher 与 Script Adapter
  - 范围：依赖装配，不注册框架 Runtime。
  - 改动：Native Runner 使用统一 Tool 执行层。
  - Spec 引用：8.6 阶段 A。
  - 验收：AC2、AC4。
- [x] 0.9 修改/新增后端 Tool 测试 — 验证权限、普通 Tool、脚本兼容、缓存与事件
  - 文件：`tests/test_skill_execution.py`、`tests/test_agent_runtimes.py`
  - 范围：每个测试文件仅覆盖对应模块；不访问真实模型网络。
  - 改动：建立统一 Tool 层回归基线。
  - Spec 引用：8.6 首期完成标准。
  - 验收：AC2、AC4、AC5、AC6。

## 模块 1：Agent Runtime 抽象（P0）

- [x] 1.1 新增 `src/snow_grass/agent/runtime.py` — 定义 AgentRunRequest、AgentRuntimeInfo 和 AgentRuntime 协议
  - 范围：Runtime 无关类型和 `stream()` 契约。
  - 改动：统一 Native 与框架输入输出。
  - Spec 引用：功能点 4；8.3。
  - 验收：AC4、AC5。
- [x] 1.2 新增 `src/snow_grass/agent/runtime_registry.py#AgentRuntimeRegistry` — 注册、列举和解析 Runtime
  - 范围：`register/list/resolve` 与不可用原因。
  - 改动：支持 `/agent-runtimes` 和会话分发。
  - Spec 引用：功能点 1、4。
  - 验收：AC1、AC5。
- [x] 1.3 新增 `src/snow_grass/agent/native_runtime.py#NativeAgentRuntime` — 包装现有 AgentRunner
  - 范围：请求转换与 Runtime 元数据。
  - 改动：旧行为默认 `native`。
  - Spec 引用：功能点 2、4。
  - 验收：AC2、AC4。
- [x] 1.4 新增 `src/snow_grass/agent/openai_agents_runtime.py#OpenAIAgentsRuntime` — 适配 OpenAI Agents SDK
  - 范围：DeepSeek Chat Completions、Skill instructions、工具转换和事件映射。
  - 改动：禁用默认 OpenAI Trace 上传，失败不回退 Native。
  - Spec 引用：功能点 5、6；8.1。
  - 验收：AC4、AC5、AC6、AC7。
- [x] 1.5 修改 `pyproject.toml#dependencies` — 锁定 OpenAI Agents SDK 依赖
  - 范围：Python 3.11+ 可安装版本及测试依赖兼容。
  - 改动：不引入 LiteLLM。
  - Spec 引用：8.1 Framework 模型适配。
  - 验收：AC1、AC4。
- [x] 1.6 修改 `src/snow_grass/main.py#create_app` — 注册 Native 与 OpenAI Agents Runtime
  - 范围：Runtime Registry 装配和可用状态。
  - 改动：框架依赖/配置不可用时保留 Native。
  - Spec 引用：功能点 1、5。
  - 验收：AC1、AC2、AC5。

## 模块 2：会话、持久化与 API（P0）

- [x] 2.1 修改 `src/snow_grass/persistence/models.py#SessionRecord` — 保存 runtime_id 与运行指标
  - 范围：会话字段及 Agent Run 记录结构。
  - 改动：支持 Runtime、耗时、Token、工具次数和状态归因。
  - Spec 引用：功能点 2、8。
  - 验收：AC1、AC2、AC7。
- [x] 2.2 修改 `src/snow_grass/persistence/database.py` — 增加 SQLite 兼容迁移
  - 范围：已有数据库字段补齐。
  - 改动：历史会话 `runtime_id` 默认 `native`。
  - Spec 引用：8.5 历史会话兼容。
  - 验收：AC2。
- [x] 2.3 修改 `src/snow_grass/persistence/repository.py#ChatRepository` — 读写 Runtime 和 Run 指标
  - 范围：创建/读取会话及执行完成/失败记录。
  - 改动：会话值作为 Runtime 事实来源。
  - Spec 引用：功能点 2、8。
  - 验收：AC1、AC5、AC7。
- [x] 2.4 修改 `src/snow_grass/api/schemas.py` — 增加 Runtime 目录和会话字段
  - 范围：CreateSession、SessionResponse、RuntimeInfo Schema。
  - 改动：请求默认 `native`，消息请求不得覆盖 Runtime。
  - Spec 引用：API 接口清单。
  - 验收：AC1、AC2、AC3。
- [x] 2.5 修改 `src/snow_grass/api/router.py` — 增加 Runtime 目录并按会话分发
  - 范围：`GET /agent-runtimes`、创建会话和消息流。
  - 改动：统一 SSE 增加 Runtime 和指标字段，失败不静默回退。
  - Spec 引用：功能点 1、2、4、8。
  - 验收：AC1–AC7。

## 模块 3：Sedum 前端运行时切换（P0/P1）

- [x] 3.1 修改 `src/api/snowGrassApi.js` — 增加 Runtime 目录 API 和创建会话参数
  - 范围：`getAgentRuntimes`、`createSession`、Session 映射。
  - 改动：流消息不发送可覆盖的 runtimeId。
  - Spec 引用：功能点 1、2。
  - 验收：AC1、AC3。
- [x] 3.2 修改 `src/page/agent/AgentPage.jsx` — 管理 Runtime 状态和会话切换
  - 范围：初始化、历史会话恢复、新会话和 `sendMessage`。
  - 改动：已有消息时切换创建新会话并保留输入。
  - Spec 引用：功能点 2、3。
  - 验收：AC1、AC2、AC3。
- [x] 3.3 修改 `src/component/agent/components/ChatComposer.jsx` — 增加自研/框架分段控件
  - 范围：Runtime props、禁用状态和不可用原因。
  - 改动：框架标签明确显示 OpenAI Agents SDK。
  - Spec 引用：UI/UX；功能点 1、2、3。
  - 验收：AC1、AC3。
- [x] 3.4 修改 `src/component/agent/components/MessageTimeline.jsx` — 展示 Runtime 和统一 Tool 轨迹
  - 范围：Run 头部、Tool started/completed/failed 和指标。
  - 改动：不展示私有思维链。
  - Spec 引用：功能点 7、8。
  - 验收：AC4、AC5、AC6、AC7。
- [x] 3.5 修改 Agent 样式文件 — 增加 Runtime 控件和轨迹标识样式
  - 范围：以当前 Agent 页面实际样式入口为准。
  - 改动：适配窄屏、不可用状态和历史会话锁定提示。
  - Spec 引用：UI/UX。
  - 验收：AC1、AC3、AC6。

## 全量验证

- Snow Grass：运行 Tool、Skill、Session、API、Runtime 单元测试与现有完整测试。
- Sedum：运行 lint/build，并人工核对新建会话、历史会话及 Runtime 不可用状态。
- 不启动 Snow Grass 服务；需要端到端联调时另行取得用户授权。
